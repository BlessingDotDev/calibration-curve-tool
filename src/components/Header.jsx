import "./Header.css"

function Header() {
  return (
    <header className="site-header">
      <button>
        logo
      </button>

      <nav>
        <button>
          Menu
        </button>
      </nav>
    </header>
  );
}

export default Header;